
程序运行：jpy-server-windows-amd64.exe
运行后会监听1001（HTTP）和1002（WS）端口，即你可以通过ws来调用也可以通过api调用。
通过该程序接口可以调用集控平台的基础能力（如获取设备列表，下发安装应用，手机控制，端口映射）
访问 http://127.0.0.1:1001/doc 查看接口文档。
提供了examples调用例子

API_KEY：
	在集控平台的右上角“我的头像”->“密钥管理”->“当前密钥”中获取。

使用ACCBOT开启调试端口映射：
accbot主要通过19011调试，可通过调用接口映射指定手机的19011端口到本地19011端口，实现对手机的控制。
以下提供2种方法

第一种：
	运行程序并登录http://127.0.0.1:1001/
	直接找到对应设备，点击“开发者模式” 即可自动调用接口映射19011端口到本地19011端口。

第二种：
运行程序后，通过调用接口完成端口映射：
await post("/api/connect", {
        key: API_KEY,
        deviceId: 79619,		//设备ID
        tbYunJiUserDeviceId: 10043,	//用户设备ID
        localPort: 19011,	
        phonePort: 19011
});


映射后即可通过localhost:19011来调试accbot
更多接口调用可以直接访问 
http://127.0.0.1:1001/ 
或
http://127.0.0.1:1001/doc
或
examples调用例子

